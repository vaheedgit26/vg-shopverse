apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mysql.externalNameService }}  
  namespace: {{ .Values.namesoace }}             # shopverse-dev
spec:
  type: ExternalName
  externalName: {{ .Values.mysql.host | quote }}
